mixin icon(options = {})
  -
    const iconName = options.iconName;
    const iconWidth = options.iconWidth;
    const iconHeight = options.iconHeight;

  svg.icon(width=iconWidth, height=iconHeight, aria-hidden="true")&attributes(attributes)
    use(xlink:href="img/sprite_auto.svg#" + iconName)

//- &attributes(attributes) позволяет вызывать миксин с доп классом и аттрибутами
//- +icon("icon-close", 15, 15).some-class
